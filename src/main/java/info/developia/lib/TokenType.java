package info.developia.lib;

public enum TokenType {
    BRACKET_OPEN,
    BRACKET_CLOSE,
    IDENTIFIER,
    SELECT, FROM, WHERE,
    COMMA, STAR, EQUALS,
    VALUE_STRING,
    SEMICOLON,
    EOF,
}
